{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campu.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('campus_index') }}">back to list</a>

    <a href="{{ path('campus_edit', {'id': campu.id}) }}">edit</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
